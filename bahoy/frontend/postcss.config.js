/**
 * Bahoy - Configuración de PostCSS
 * PostCSS es un procesador de CSS que permite usar plugins como Tailwind y Autoprefixer.
 */

module.exports = {
  plugins: {
    // Tailwind CSS para utility-first CSS
    tailwindcss: {},
    // Autoprefixer para agregar prefijos de navegadores automáticamente
    autoprefixer: {},
  },
}
